An order has been placed!

Manage Order <%= appUrl %>/user/orders/manage;order=<%= order._id %>

#<%= order._id %>

Ordered <%= order.date %>

<% if (order.orderType && order.orderType === 'seller') { %>Buyer <%= order.buyerName %> <% } %>

----------------------------------------------------------
<% order.products.forEach(function(product) { %>
<%= product.name %> x <%= product.count %> <%= product.unit_size %> @ <%= product.price %> | $<%= product.total %>
<% }); %>
----------------------------------------------------------

Delivery Address

<%= order.shipping_address.nickname %>


<%= order.shipping_address.address_line_1 %><% if(order.shipping_address.address_line_2) { %>, <%= order.shipping_address.address_line_2 %><% } %>

<%= order.shipping_address.city %>, <%= order.shipping_address.state %> <%= order.shipping_address.zip %>


<%= order.delivery_time %>

----------------------------------------------------------

Order Total | $<%= order.summary.subtotal %>

----------------------------------------------------------

Manage Order <%= appUrl %>/user/orders/manage;order=<%= order._id %>

